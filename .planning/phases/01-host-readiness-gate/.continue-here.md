---
context: phase
phase: 01-host-readiness-gate
plan: 6
task: 3
total_tasks: 3
status: in_progress
last_updated: 2026-07-27T09:21:48.001Z
---

<current_state>
Phase 01, Plan 01-06 is paused at the blocking-human gate before Task 3 live
acceptance. Plans 01-01 through 01-05 are complete. Plan 01-06 Tasks 1-2 are
committed and pushed; there is intentionally no 01-06-SUMMARY.md yet.

The immutable pre-reboot archive verifies from contained bytes:

- Run: `run-a0d9c6cc52b55dfaa6f15a9a176be98ed7ac1691cca76913115aa0870cb2127f`
- Archived binary SHA-256:
  `9662e8ee6a8196ee81f2011ce60fe6aba9712db7960e3640c19ba22fe01e677e`
- Archive result: `status=verified`, `evidence_status=fail`

The source-independent Wave 6 state passes 50 unit, 37 process, and 8 mapping
tests under the default-parallel runner.
</current_state>

<completed_work>

Completed Tasks:

- Task 1: Carry selected output through production evidence — Done in
  `cb32ed0` and `c717603`.
- Task 2: Close process boundaries and compatibility — Done in `f8cf2b9` and
  `242b36f`.
- Task 3 precondition: Immutable archive verification — Done.

The production doctor now has explicit `--output`, bounded live
query/recheck, strict `selected-output.v1` semantics, adversarial process
coverage, and permanent foundation/archive compatibility gates.
</completed_work>

<remaining_work>

- Operator: reboot into a kernel whose NVIDIA module matches userspace
  `610.43.03`.
- Operator: log into one real local physical Xorg session.
- Continuation: verify the corrected session/runtime before any admission.
- Continuation: run discovery and enumerate exact output candidates.
- Operator: choose one candidate name exactly as `REPLAY_HOST_OUTPUT`.
- Continuation: run selected-output proof and require HOST-01/HOST-02 PASS,
  HOST-03/HOST-04 UNPROVEN, and overall exit 2.
- Continuation: commit Task 3, create/commit `01-06-SUMMARY.md`, update
  STATE/ROADMAP/REQUIREMENTS, run post-wave gates, then proceed to Plan 01-07.
</remaining_work>

<decisions_made>

- Use `/opt/cuda/targets/x86_64-linux/include` as
  `REPLAY_NVML_SDK_ROOT`; the operator confirmed lawful access and applicable
  terms.
- Never infer or default an output. Only an exact operator-selected X11 output
  with one unique XRandR-to-DRM-to-PCI-to-NVML relation may pass HOST-02.
- Never mutate or regenerate the pre-reboot archive. It is immutable historical
  proof, not a rolling cache.
- Run the default-parallel test suite. The prior Linux `ETXTBSY` copied-binary
  race was structurally fixed in `164c365` and its records were closed in
  `3bee83c`.
</decisions_made>

<blockers>

- Current running kernel: `7.0.10-2-cachyos` (its module tree is no longer
  installed).
- Loaded NVIDIA module: `610.43.02`.
- NVIDIA userspace NVML: `610.43.03`.
- Current session: ID 3, Wayland, `DISPLAY=:0`.
- Installed corrected choices:
  - `linux-cachyos 7.1.4-1` with NVIDIA module `610.43.03` — preferred.
  - `linux-cachyos-lts 6.18.38-2` with NVIDIA module `610.43.03`.
  - Installed `7.0.3-1-cachyos` has no NVIDIA module and must not be selected.
- Plasma Login Manager defaults to `plasma` Wayland. Installed real-Xorg
  sessions are Cinnamon and i3; Cinnamon is the easiest prototype choice.
</blockers>

## Required Reading (in order)

1. `.planning/HANDOFF.json` — canonical structured checkpoint state.
2. `.planning/phases/01-host-readiness-gate/01-06-PLAN.md` — Task 3 commands,
   acceptance criteria, and compatibility requirements.
3. `.planning/phases/01-host-readiness-gate/01-VALIDATION.md` — immutable
   archive and corrected-host operator procedure.
4. `.planning/phases/01-host-readiness-gate/01-05-SUMMARY.md` — mapping
   contract and resolved parallel-test issue.
5. `.planning/STATE.md` and `.planning/PROJECT.md` — phase position and locked
   project decisions.

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Partial-plan redispatch | Tasks 1-2 have production commits but no summary because Task 3 is at a documented human gate; starting 01-06 from scratch would duplicate work. | advisory | Spawn a fresh continuation with the four completed commits and resume at Task 3. |
| Implicit output selection | Picking the first or primary-looking output would make HOST-02 evidence non-authoritative. | advisory | Enumerate candidates, show them to the operator, and require one exact name before proof. |
| Historical archive mutation | Replacing the pre-reboot archive would destroy the before/after proof. | advisory | Run `verify-archive` only; all post-reboot evidence goes to a new path. |
| Serial-only test acceptance | A copied-executable test once passed serially but failed in parallel with `ETXTBSY`. | advisory | Keep the `exec` handshake mutex and run the default-parallel full suite at every post-wave gate. |

## Infrastructure State

- Git branch: `main`; all implementation/checkpoint commits are pushed to
  `origin/main`.
- No server, watcher, external async job, or executor remains active.
- `REPLAY_NVML_SDK_ROOT=/opt/cuda/targets/x86_64-linux/include`.
- Display manager: Plasma Login Manager (`plasmalogin`), default session
  `plasma` (Wayland).
- Available real-Xorg sessions:
  `/usr/share/xsessions/cinnamon.desktop` and i3 variants.
- The machine must be rebooted by the operator; no agent is authorized to
  perform the reboot or change the login session.

<context>
The host-readiness implementation is halfway through the ten-plan phase.
Everything source-independent for selected-output admission is complete and
verified. The next result must be a real corrected-host observation, not a
fixture or inferred state. Preserve all fail-closed semantics while resuming.
</context>

<next_action>
Start with: reboot into `linux-cachyos 7.1.4-1`, choose Cinnamon at Plasma
Login Manager so `XDG_SESSION_TYPE=x11`, reopen this Codex task, and reply
`ready`.
</next_action>
