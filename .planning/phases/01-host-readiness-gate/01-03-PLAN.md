---
phase: 01-host-readiness-gate
plan: 03
type: execute
wave: 3
depends_on:
  - "01-02"
files_modified:
  - build.rs
  - native/nvml_abi_oracle.c
  - src/lib.rs
  - src/probe.rs
  - src/local_xorg.rs
  - src/native_nvml.rs
  - tests/fixtures/current-wayland-driver-mismatch.json
  - tests/fixtures/host01-session-spoofing.json
  - tests/host_doctor_cli.rs
autonomous: false
requirements:
  - HOST-01
user_setup:
  - service: nvidia-nvml-header-source
    why: "Live NVML admission requires an operator-controlled official nvml.h and an explicit lawful-source assertion; a runtime shared object is not source authority."
    env_vars:
      - name: REPLAY_NVML_SDK_ROOT
        source: "Absolute readable path to an official NVIDIA source/include tree containing nvml.h after the operator has accepted applicable terms outside this workflow."
    dashboard_config:
      - task: "Confirm the source identity and applicable terms separately; do not ask the executor to download a header, install a package, or accept terms."
        location: "Operator-controlled NVIDIA source and local filesystem"
must_haves:
  truths:
    - "Local-Xorg admission is conjunctive across an active local non-remote logind session, AF_UNIX X transport, SO_PEERCRED, /proc peer executable resolving to real Xorg, and a successful x11rb setup/RandR exchange."
    - "Environment variables, DISPLAY, an X socket path, or Xwayland/nested/remote servers cannot satisfy the local-Xorg predicate (D-02, D-06)."
    - "Live NVML passes only after official-header ABI comparison and the complete libnvidia-ml.so.1 load/initialize/version/device/UUID/PCI/shutdown sequence."
    - "The current Wayland plus 610.43.02/610.43.03 mismatch remains a named live/fixture FAIL and never degrades into a generic encoder error (D-06)."
    - "uinput and render-node access, coarse physical-output presence, NVIDIA kernel/userspace identity, and remediation reasons are current-run observations in host-foundation.v1."
    - "Selected-output correlation, NvFBC, and NVENC remain unproven, so overall G0 cannot pass (D-01)."
  artifacts:
    - path: "src/local_xorg.rs"
      provides: "Fail-closed local physical Xorg identity proof and HOST-01 source-neutral checks"
      exports: ["HostFoundationEvidenceV1", "LocalXorgEvidenceV1", "prove_local_xorg"]
    - path: "src/native_nvml.rs"
      provides: "Official-header-gated NVML source/runtime provider"
      exports: ["NvmlProvider", "NativeNvmlProvider", "LiveUnavailableNvmlProvider", "NvmlEvidenceV1"]
    - path: "native/nvml_abi_oracle.c"
      provides: "Project-owned comparisons compiled against operator-provided nvml.h"
    - path: "tests/fixtures/current-wayland-driver-mismatch.json"
      provides: "Known current-host negative observation fixture"
  key_links:
    - from: "src/probe.rs"
      to: "src/local_xorg.rs"
      via: "the bounded session worker supplies logind, socket peer, process, setup, and RandR observations"
      pattern: "ProbeId::Session|prove_local_xorg"
    - from: "src/native_nvml.rs"
      to: "native/nvml_abi_oracle.c"
      via: "every used Rust declaration/layout/constant/status is compared with the supplied official header"
      pattern: "nvml_abi|NativeNvmlProvider"
    - from: "src/lib.rs"
      to: "src/model.rs"
      via: "host-foundation.v1 is populated through the existing generic extension record without changing V1 base fields"
      pattern: "host-foundation.v1|G0ExtensionRecordV1"
  prohibitions:
    - statement: "No environment-only, API-presence-only, Xwayland, nested, TCP, proxy, or remote session can pass local Xorg."
      status: "resolved"
      verification: "Spoofing fixtures and production process tests require the complete joined predicate."
    - statement: "No NVML library/symbol presence can pass without verified source ABI and a complete runtime sequence."
      status: "resolved"
      verification: "Source-absence, ABI mismatch, missing-symbol, partial-runtime, timeout, and shutdown-failure cases remain failed."
    - statement: "No archive, output selection, capture, encode, reboot, session change, driver install, permission change, or module mutation is owned by this plan."
      status: "resolved"
      verification: "Files and worker operation allowlists are limited to read-only HOST-01 proof and requested evidence output inherited from Plan 01-02."
---

<objective>
Add native local-Xorg and source-verified NVML proof to the existing doctor,
without taking archive or later media responsibility.

Purpose: Distinguish the real local Xorg/NVIDIA runtime from spoofed session
metadata and library presence, and make the current host's known failure
specific and reproducible (D-01, D-02, D-06, D-07, D-08).

Output: Conjunctive Xorg evidence, official-source-backed NVML ABI/runtime
evidence, current-host and adversarial fixtures, and production process
assertions in `host-foundation.v1`.
</objective>

<execution_context>
@/home/finn/.codex/gsd-core/workflows/execute-plan.md
@/home/finn/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/research/SUMMARY.md
@.planning/research/STACK.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
@.planning/phases/01-host-readiness-gate/01-CONTEXT.md
@.planning/phases/01-host-readiness-gate/01-02-SUMMARY.md

Consume the existing bounded worker, parent evaluator, V1 envelope, shared
digest, and atomic store. Add no Cargo dependency and do not alter the V1 base
decoder.
</context>

## Artifacts this phase produces

| Path | Contract |
|---|---|
| `src/local_xorg.rs` | logind + AF_UNIX + SO_PEERCRED + `/proc/<pid>/exe` + x11rb setup/RandR proof |
| `src/native_nvml.rs` | No-header unavailable provider and official-header-gated live NVML provider |
| `native/nvml_abi_oracle.c` | Includes but never copies operator-provided `nvml.h` |
| `build.rs` | Conditional source/oracle compile; no-header fixture build remains available |
| `tests/fixtures/current-wayland-driver-mismatch.json` | Wayland, Xwayland, loaded 610.43.02, userspace 610.43.03, uinput/render-access facts |
| `tests/fixtures/host01-session-spoofing.json` | Remote/inactive/multiple sessions, TCP, wrong peer, Xwayland, Xephyr/Xnest/Xvnc/Xdummy, malformed setup, and native failure cases |

`host-foundation.v1` is a known strict extension payload. Its schema validator
accepts explicit `unproven`, `fail`, or `pass` subchecks but does not change
the generic extension record or any V1 base member. It records exact current
observations and source digests; it never records raw environment, native
stderr, operator paths, or proprietary header content.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Prove one local-Xorg-shaped path through the production doctor</name>
  <read_first>
    - `.planning/phases/01-host-readiness-gate/01-02-SUMMARY.md` — actual worker, evaluator, store, and CLI contracts
    - `.planning/phases/01-host-readiness-gate/01-CONTEXT.md` — D-01, D-02, D-06 through D-09
    - `.planning/REQUIREMENTS.md` — HOST-01 failure list
    - `.planning/research/PITFALLS.md` — wrong-session and API-presence pitfalls
  </read_first>
  <files>src/lib.rs, src/probe.rs, src/local_xorg.rs, tests/fixtures/host01-session-spoofing.json</files>
  <behavior>
    - One active local non-remote user session whose connected AF_UNIX X peer is real Xorg and whose setup/RandR exchange succeeds produces only the local-Xorg subproof.
    - Fake XDG/DISPLAY values fail when logind, transport, peer credentials/process, or protocol evidence disagrees.
    - Missing uinput/render access or coarse physical output presence produces a stable actionable HOST-01 failure.
  </behavior>
  <action>
Extend the bounded Session worker to invoke absolute `/usr/bin/loginctl`,
enumerate candidate sessions, and query exact Id/User/Seat/TTY/Display/Remote/
RemoteHost/Type/Class/Active/State/Leader/Scope fields. Require exactly one
active local non-remote user session for the current euid whose leader/process
relationship contains the doctor.

Inspect the actual connected X11 descriptor, require AF_UNIX, query Linux
SO_PEERCRED, resolve `/proc/<peer-pid>/exe`, require a regular real Xorg
executable, and reject Xwayland, nested, VNC, dummy, proxy, and TCP paths.
Complete the subproof with x11rb setup vendor/release/protocol/root facts plus
RandR extension/version/provider/output enumeration. Query `/dev/uinput`,
`/dev/dri/renderD*`, loaded NVIDIA module version, and connected physical DRM
connector presence read-only. Provider/output/GPU correlation remains
unproven for Plans 01-05/01-06.
  </action>
  <verify>
    <automated>bash -c 'cargo test --locked local_xorg_ &amp;&amp; cargo test --locked host01_source_neutral_ &amp;&amp; cargo test --locked bounded_probe_session_'</automated>
  </verify>
  <acceptance_criteria>
    1. Admission joins logind, AF_UNIX, SO_PEERCRED, peer executable, X11 setup, and RandR facts.
    2. Environment strings alone cannot influence the pass predicate.
    3. Device/output checks are read-only and source-neutral.
    4. A local-Xorg-shaped fixture still cannot make live G0 pass.
    5. Output/GPU, NvFBC, and NVENC blockers remain explicit.
  </acceptance_criteria>
  <done>The production path distinguishes real local Xorg from spoofed, Xwayland, nested, TCP, and remote state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Authenticate the NVML source ABI and exercise the complete runtime</name>
  <precondition>`REPLAY_NVML_SDK_ROOT` is an absolute readable operator-controlled official source tree containing `nvml.h`, and the operator separately confirms lawful source access and applicable terms.</precondition>
  <read_first>
    - `$REPLAY_NVML_SDK_ROOT/nvml.h` — operator-provided official declaration source
    - `.planning/research/STACK.md` — NVML/runtime and driver identity expectations
    - `src/probe.rs`, `src/local_xorg.rs`, `src/lib.rs` — Task 1 and inherited worker/evaluator contracts
    - `src/digest.rs` — shared source-file digest API
  </read_first>
  <files>build.rs, native/nvml_abi_oracle.c, src/native_nvml.rs, src/probe.rs, src/lib.rs</files>
  <operator_gate gate="blocking-human">
The executor stops before source-derived FFI or live NVML work until the
operator supplies `REPLAY_NVML_SDK_ROOT` and confirms source identity/terms
outside this workflow. The executor does not download a header, install an
NVIDIA package, accept terms, or copy proprietary source into the repository.
  </operator_gate>
  <behavior>
    - The compiled C oracle agrees with every Rust type, size, alignment, offset, constant, function signature, and status used by the provider.
    - Header/source absence or uncertainty leaves a stable source-unavailable failure while fixture/no-header builds remain usable.
    - Only load, initialize, userspace-version query, positive device enumeration, bounded UUID/PCI query, and shutdown can pass live NVML.
  </behavior>
  <action>
Inventory the supplied header/release identity/license notice and create a
source map for every declaration used. Compile a narrow C oracle that includes
the official header and exports project-owned comparison values only; never
copy the header or operator path. Compare all Rust declarations/layouts/
constants/statuses before loading the runtime.

Use libloading 0.9.0 to open only `libnvidia-ml.so.1`, resolve only verified
symbols, initialize, obtain the userspace driver version, require a positive
device count, collect bounded UUID and canonical PCI BDF for each device, and
shutdown on every initialized path. Compare loaded-kernel and userspace
versions as exact components, not floating-point values. Hash the source file
through `sha256_file`; record its digest and public release identity only.
  </action>
  <verify>
    <automated>bash -c 'test -n "${REPLAY_NVML_SDK_ROOT:-}" &amp;&amp; test -r "$REPLAY_NVML_SDK_ROOT/nvml.h" &amp;&amp; cargo test --locked nvml_source_abi_ &amp;&amp; cargo test --locked nvml_runtime_'</automated>
  </verify>
  <acceptance_criteria>
    1. Every unsafe declaration has a source-map and C-oracle comparison.
    2. Source absence/ambiguity, ABI mismatch, missing symbol, init/query/device/shutdown failure, and timeout remain failed.
    3. Loaded kernel and userspace versions are preserved exactly.
    4. Initialized paths attempt shutdown exactly once and bounded workers are reaped.
    5. No source content/path or raw native error enters evidence.
  </acceptance_criteria>
  <done>NVML readiness is an authenticated source-plus-runtime proof, never a shared-object presence claim.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Lock HOST-01 native failures into fixture and process assertions</name>
  <read_first>
    - `src/local_xorg.rs`, `src/native_nvml.rs`, `src/probe.rs`, `src/lib.rs` — completed native predicates
    - `tests/fixtures/host01-session-spoofing.json` — Task 1 cases
    - `.planning/PROJECT.md` and `.planning/phases/01-host-readiness-gate/01-CONTEXT.md` — current host facts and D-06
  </read_first>
  <files>tests/fixtures/current-wayland-driver-mismatch.json, tests/fixtures/host01-session-spoofing.json, tests/host_doctor_cli.rs</files>
  <behavior>
    - The current-host fixture emits `SESSION_NOT_XORG` and `NVIDIA_VERSION_MISMATCH` with the exact known versions.
    - Every local-Xorg/NVML partial success remains failed through the production binary, evaluator, atomic store, and readback.
    - Secret, path, EDID, native stderr, pointer, duplicate, timeout, cached-PASS, and injected-status sentinels do not enter admitted evidence.
  </behavior>
  <action>
Complete the negative matrix for current Wayland/Xwayland, multiple/remote/
inactive logind sessions, wrong/missing peer credentials, nested/remote X
servers, malformed setup/RandR, kernel/userspace adjacency/equality, failed
NVML stages, zero devices, missing uinput/render/output, timeouts, abnormal
children, and cleanup failures. Run each through the production process path.

Assert stable ordered reason codes with requested/observed values and one
bounded remediation, while keeping raw environment, authorization material,
native messages, and source paths out of output. Prove `host-foundation.v1`
round-trips strictly and overall G0 retains the selected-output, capture, and
encode blockers.
  </action>
  <verify>
    <automated>bash -c 'cargo fmt --check &amp;&amp; cargo clippy --locked --all-targets -- -D warnings &amp;&amp; cargo test --locked host01_ &amp;&amp; cargo test --locked --test host_doctor_cli host01_native_'</automated>
  </verify>
  <acceptance_criteria>
    1. The known Wayland and 610.43.02/610.43.03 mismatch is specific and deterministic.
    2. Every partial/spoofed/native timeout path fails and cleans up.
    3. HOST-01 known-extension validation is strict without a V1 base change.
    4. No fixture or current partial state can clear later blockers or G0.
  </acceptance_criteria>
  <done>HOST-01 native behavior is source-backed, adversarially tested, and ready for a fresh pre-reboot archive.</done>
</task>

</tasks>

<threat_model>
## Security Level

OWASP ASVS L1; high-severity findings block completion.

## Trust Boundaries

| Boundary | Description |
|---|---|
| Environment/logind/X socket → session worker | Spoofable host session identity enters admission |
| Operator nvml.h → C oracle/Rust FFI | External declarations and license provenance determine unsafe ABI |
| NVML library → bounded worker | Native calls may hang, fail, or return malformed identity |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-03-01 | Spoofing | Local Xorg | high | mitigate | Conjunctive logind, AF_UNIX, SO_PEERCRED, real peer executable, X11 setup/RandR, adversarial fixtures. |
| T-01-03-02 | Tampering | NVML ABI/source | high | mitigate | Explicit source root/assertion, source map/digest, C oracle, no-header unavailable provider. |
| T-01-03-03 | Denial of Service | Helpers/native calls | high | mitigate | Existing per-probe bounds, child kill/wait, NVML shutdown, partial-init matrix. |
| T-01-03-04 | Information Disclosure | Host/native diagnostics | high | mitigate | Typed allowlist, bounded remediation, no raw environment/stderr/source/path/pointers. |
| T-01-03-05 | Elevation of Privilege | Host checks | high | mitigate | Query/read-only operations; no reboot/session/driver/module/permission/device mutation. |
</threat_model>

<verification>
1. `cargo fmt --check`
2. `cargo clippy --locked --all-targets -- -D warnings`
3. `cargo test --locked host01_`
4. With operator-provided official source: run `nvml_source_abi_` and `nvml_runtime_`.
5. Do not create an archive in this plan.
</verification>

<success_criteria>
- Real local Xorg and live NVML are proven by complete source/runtime chains.
- The current Wayland/driver mismatch is a first-class named FAIL.
- uinput/render/output presence is checked read-only.
- Selected-output correlation, capture, encode, and overall G0 remain open.
</success_criteria>

## Source coverage audit

| SOURCE | ID | Feature/requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Real Xorg/NVIDIA readiness | 01-03 | COVERED | Local-Xorg and live NVML proof establish the native foundation. |
| REQ | HOST-01 | Block session, driver/NVML, output, uinput, and render failures | 01-03 | COVERED | Conjunctive source/runtime probes and process assertions. |
| RESEARCH | HostProbe/System ABI | Actual Xorg/NVML path and exact identities | 01-03 | COVERED | Peer/process/protocol proof plus source-backed runtime sequence. |
| RESEARCH | Pitfalls 1/4 | No API presence or wrong-session claim | 01-03 | COVERED | All partial/spoofed cases remain failed. |
| CONTEXT | D-01 | Complete fail-closed G0 | 01-03 | COVERED | Later extension blockers remain. |
| CONTEXT | D-02 | Exact runtime path | 01-03 | COVERED | Full Xorg and NVML chains required. |
| CONTEXT | D-06 | Known current failing host | 01-03 | COVERED | Exact live-shaped fixture and process reasons. |
| CONTEXT | D-07 | No mutation | 01-03 | COVERED | Read-only source/runtime checks only. |
| CONTEXT | D-08 | Timeouts/cleanup | 01-03 | COVERED | Bounded workers and NVML shutdown matrix. |
| CONTEXT | D-09 | Durable exact evidence | 01-03 | COVERED | Strict host-foundation.v1 payload in final envelope. |

Output mapping, capture, NVENC, and deferred product work remain assigned to
later plans. No source-audit item is missing.

<output>
Create `.planning/phases/01-host-readiness-gate/01-03-SUMMARY.md` when done.
</output>
