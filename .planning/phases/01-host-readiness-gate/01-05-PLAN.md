---
phase: 01-host-readiness-gate
plan: 05
type: execute
wave: 5
depends_on: ["01-04"]
files_modified:
  - .planning/phases/01-host-readiness-gate/01-OUTPUT-GPU-MAPPING-SPIKE.md
  - src/lib.rs
  - src/model.rs
  - src/output_mapping.rs
  - tests/fixtures/host02-output-topologies.json
autonomous: true
requirements: [HOST-02]
must_haves:
  truths:
    - "01-OUTPUT-GPU-MAPPING-SPIKE.md exists before production live mapping and records sources, field namespaces, observed cardinalities, executable commands, and a concrete unique-join predicate (D-02, D-03)."
    - "No XRandR output XID is assumed equal to a DRM connector_id."
    - "The pure mapper joins output/provider/EDID digest/source-verified connector metadata/active timing to DRM device PCI BDF and then one NVML BDF/UUID."
    - "Missing, duplicate, identical-display, ambiguous, or topology-changing relations fail; no candidate order supplies a default."
  artifacts:
    - path: ".planning/phases/01-host-readiness-gate/01-OUTPUT-GPU-MAPPING-SPIKE.md"
      provides: "Source/host feasibility and exact mapping predicate"
    - path: "src/output_mapping.rs"
      provides: "Pure output/GPU correlation"
      exports: ["OutputTopologyV1", "SelectedOutputV1", "prove_output_gpu_mapping"]
    - path: "tests/fixtures/host02-output-topologies.json"
      provides: "Positive, namespace-disjoint, identical-display, ambiguity, timing, and race cases"
  key_links:
    - from: "src/output_mapping.rs"
      to: "src/digest.rs"
      via: "raw EDID is compared in memory and persisted only as Sha256DigestV1"
      pattern: "sha256_bytes|edid_sha256"
    - from: "src/output_mapping.rs"
      to: "src/native_nvml.rs"
      via: "resolved DRM PCI BDF must equal exactly one same-run NVML BDF/UUID"
      pattern: "pci_bdf|nvml_uuid"
  prohibitions:
    - statement: "No XRandR XID/DRM connector_id equality, first-output choice, best-effort match, panorama, virtual output, or fallback mapping is admitted."
      status: "resolved"
      verification: "Namespace-disjoint and ambiguity fixtures exercise the pure mapper."
    - statement: "No raw EDID, X authorization data, or production live integration is persisted in this plan."
      status: "resolved"
      verification: "Fixture/model tests inspect digests and pure observations only; live CLI wiring belongs to Plan 01-06."
    - statement: "No base V1 decoder or archive verifier change belongs to this isolated spike/pure-mapper plan."
      status: "resolved"
      verification: "The file/task scope is limited to the mapping spike, pure models, mapper, and fixtures; compatibility is rerun at Plan 01-06 integration."
---

<objective>
Prove output-to-GPU correlation feasibility and freeze the pure fail-closed
mapping contract before live CLI integration.

Purpose: Replace namespace guessing with an executable, source-backed unique
relation for exactly one physical output (D-01, D-02, D-03, D-09).

Output: Mapping spike, pure model/implementation, and exhaustive topology
fixtures.
</objective>

<execution_context>
@/home/finn/.codex/gsd-core/workflows/execute-plan.md
@/home/finn/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/REQUIREMENTS.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
@.planning/phases/01-host-readiness-gate/01-CONTEXT.md
@.planning/phases/01-host-readiness-gate/01-04-SUMMARY.md
</context>

## Artifacts this phase produces

| Path | Contract |
|---|---|
| `01-OUTPUT-GPU-MAPPING-SPIKE.md` | Primary-source identities, field namespaces/units, reference commands/results, relation cardinalities, exact predicate, ambiguity and NV-CONTROL disposition |
| `src/model.rs` | Output name bytes/display, exact refresh rational, signed origin, provider/DRM/PCI/NVML proof types; V1 base unchanged |
| `src/output_mapping.rs` | Pure unique selection/correlation and topology token |
| `tests/fixtures/host02-output-topologies.json` | Observation-only mapping matrix |

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Execute and record the output/GPU feasibility spike</name>
  <read_first>
    - `.planning/phases/01-host-readiness-gate/01-CONTEXT.md` — D-01 through D-03 and D-09
    - `.planning/research/ARCHITECTURE.md` — HostProbe/display ownership boundary
    - `.planning/research/PITFALLS.md` — wrong-session/fallback risks
    - `.planning/phases/01-host-readiness-gate/01-04-SUMMARY.md` — actual Xorg/NVML/digest contracts and immutable archive
  </read_first>
  <files>.planning/phases/01-host-readiness-gate/01-OUTPUT-GPU-MAPPING-SPIKE.md, tests/fixtures/host02-output-topologies.json</files>
  <behavior>
    - Unequal XRandR XID and DRM connector_id can still map through the complete relation.
    - Duplicate EDID with insufficient metadata, multiple providers/connectors/BDFs/NVML matches, or absent required facts produces BLOCKED_AMBIGUOUS.
    - A non-Xorg current command records BLOCKED_PRE_REBOOT_XORG rather than substituting fixture success.
  </behavior>
  <action>
Create the spike first. Record exact x11rb RandR request/reply semantics, X.Org
provider/output/property semantics, Linux DRM connector/device links, and the
existing source-verified NVML PCI/UUID contract. Inventory EDID, raw output
name, connector type/name/number, active timing, provider membership, DRM
connector name/ID, device symlink, PCI BDF, NVML BDF, and UUID with their
separate namespaces and observed cardinalities.

Define the pass predicate: one explicitly named connected active physical
XRandR output belongs to one provider; exact EDID digest plus available
source-verified connector metadata and timing select one enabled DRM connector;
its device link resolves to one canonical PCI BDF; exactly one current NVML
device has that BDF/UUID. Record PASS or named BLOCKED, never an assumption.
Keep NV-CONTROL `NOT_REQUIRED` unless the spike proves a lawful, bounded,
query-only need.
  </action>
  <verify><automated>bash -c 'test -s .planning/phases/01-host-readiness-gate/01-OUTPUT-GPU-MAPPING-SPIKE.md &amp;&amp; cargo test --locked host02_mapping_spike_'</automated></verify>
  <acceptance_criteria>
    1. Sources, fields, namespaces, units, cardinalities, command/result, and exact predicate are recorded.
    2. XID and connector ID are explicitly disjoint namespaces.
    3. Ambiguity and identical-display cases do not guess.
    4. Raw EDID is never stored.
  </acceptance_criteria>
  <done>The mapping relation is executable and any unresolved relation is an explicit blocker.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the pure mapping contract and fixture matrix</name>
  <reversibility rating="costly">SelectedOutputV1 feeds capture, coordinate mapping, telemetry, and later evidence validators.</reversibility>
  <read_first>
    - `.planning/phases/01-host-readiness-gate/01-OUTPUT-GPU-MAPPING-SPIKE.md` — authoritative predicate
    - `src/digest.rs`, `src/local_xorg.rs`, `src/native_nvml.rs` — existing identity contracts
    - `tests/fixtures/host02-output-topologies.json` — Task 1 observations
  </read_first>
  <files>src/lib.rs, src/model.rs, src/output_mapping.rs, tests/fixtures/host02-output-topologies.json</files>
  <behavior>
    - Exact output-name bytes, Unicode display, negative origin, and reduced refresh rational round-trip.
    - Namespace-disjoint positive cases pass only through the full relation.
    - Duplicate/missing/ambiguous joins and topology-token changes return no selection with stable reasons.
  </behavior>
  <action>
Implement bounded pure candidate collection models and
`prove_output_gpu_mapping`. Persist raw output-name hex plus optional valid
UTF-8 display, XRandR output/CRTC/mode/provider IDs, server/config timestamps,
checked timing and reduced refresh rational, dimensions, signed origin, EDID
digest, separately named DRM connector fields, canonical PCI BDF, NVML
BDF/UUID, and proof cardinalities. The mapper consumes observations only and
performs no native query; Plan 01-06 owns live collection and recheck.
  </action>
  <verify><automated>bash -c 'cargo test --locked host02_mapping_ &amp;&amp; cargo test --locked host02_namespace_disjoint_ &amp;&amp; cargo test --locked host02_topology_token_'</automated></verify>
  <acceptance_criteria>
    1. No code compares XRandR XID with DRM connector_id.
    2. All mapping fields are bounded and exact; refresh admission is not float based.
    3. Ambiguity, missing metadata, duplicates, and topology change fail.
    4. V1 base and generic extension record remain unchanged.
  </acceptance_criteria>
  <done>The pure HOST-02 mapping contract is deterministic and ready for live integration.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| XRandR/DRM/NVML observations → pure mapper | Mutable data from separate namespaces is correlated |
| EDID/name/topology → evidence model | Variable and potentially sensitive host data is bounded |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-05-01 | Spoofing | Output ownership | high | mitigate | Full unique EDID/metadata/timing/provider/DRM/BDF/NVML relation. |
| T-01-05-02 | Tampering | Namespace confusion | high | mitigate | Typed disjoint IDs and positive tests with unequal numeric values. |
| T-01-05-03 | Information Disclosure | EDID/name | high | mitigate | SHA-256 only for EDID, bounded name encoding, no X authority. |
| T-01-05-04 | Denial of Service | Cardinality | high | mitigate | Bounded collections, checked arithmetic, ambiguity failure. |
</threat_model>

<verification>
Run HOST-02 spike/model/mapping fixture tests, fmt, clippy, and inspect the
spike for one concrete predicate and no namespace equality.
</verification>

<success_criteria>
- Mapping feasibility is recorded before live integration.
- One physical output can be correlated uniquely or fails explicitly.
- No ID guessing, raw EDID, implicit output, or fallback exists.
</success_criteria>

## Source coverage audit

| SOURCE | ID | Feature/requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Selected physical-output proof | 01-05 | COVERED | Spike and pure relation precede capture. |
| REQ | HOST-02 | Exact output identity/geometry/GPU/origin | 01-05 | COVERED | Complete model and mapping fixtures; live evidence follows in 01-06. |
| RESEARCH | HostProbe | Real X11/display/GPU identity relation | 01-05 | COVERED | Source-backed fields and fail-closed join. |
| CONTEXT | D-01 | Fail closed | 01-05 | COVERED | Ambiguity produces no selection. |
| CONTEXT | D-02 | Exact path | 01-05 | COVERED | Complete relation, not model/name presence. |
| CONTEXT | D-03 | Exactly one physical output | 01-05 | COVERED | Scalar explicit selection contract. |
| CONTEXT | D-09 | Durable exact evidence | 01-05 | COVERED | Exact bounded SelectedOutputV1 model. |

No source-audit item is missing.

<output>
Create `.planning/phases/01-host-readiness-gate/01-05-SUMMARY.md` when done.
</output>
