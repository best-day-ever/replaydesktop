---
phase: 01-host-readiness-gate
plan: 06
type: execute
wave: 6
depends_on: ["01-05"]
files_modified:
  - src/cli.rs
  - src/lib.rs
  - src/probe.rs
  - src/evidence.rs
  - src/output_mapping.rs
  - tests/host_doctor_cli.rs
  - README.md
  - .planning/phases/01-host-readiness-gate/01-VALIDATION.md
autonomous: false
requirements: [HOST-02]
user_setup:
  - service: corrected-live-xorg-host
    why: "Live HOST-02 proof requires an operator-controlled reboot into a matching NVIDIA kernel/userspace pair and a real local Xorg login after the immutable pre-reboot archive verifies."
    env_vars:
      - name: REPLAY_HOST_OUTPUT
        source: "Exact XRandR output name selected from the corrected-host discovery result."
    dashboard_config:
      - task: "After archive verification, reboot/select the matching kernel and log into real Xorg outside this workflow."
        location: "Physical Linux host"
must_haves:
  truths:
    - "The same doctor accepts exactly one --output value, discovers candidates when omitted, and never selects by order (D-03)."
    - "selected-output.v1 strictly persists the spike-proven relation, exact geometry/timing/origin, proof cardinalities, and topology token without changing V1 base."
    - "A fresh corrected-host run proves HOST-01 and HOST-02 through real logind/Xorg/NVML/XRandR/DRM evidence; environment variables cannot supply proof."
    - "The original pre-reboot archive and foundation fixture remain readable by the extended binary."
    - "Overall G0 remains FAIL because nvfbc-capture.v1 and nvenc-tuples.v1 are unproven."
  artifacts:
    - path: "src/output_mapping.rs"
      provides: "Bounded live RandR/sysfs/NVML collection and topology recheck"
    - path: "src/evidence.rs"
      provides: "Strict selected-output.v1 known-extension validation"
    - path: ".planning/phases/01-host-readiness-gate/01-VALIDATION.md"
      provides: "Archive-first corrected-host discovery/selection/readback commands"
  key_links:
    - from: "src/probe.rs"
      to: "src/output_mapping.rs"
      via: "bounded Outputs worker collects observations then invokes the pure predicate"
      pattern: "ProbeId::Outputs|prove_output_gpu_mapping"
    - from: "src/evidence.rs"
      to: "tests/fixtures/g0-envelope-v1-foundation.json"
      via: "known selected-output validation is additive and the original V1 fixture remains accepted"
      pattern: "selected-output.v1|g0_v1_foundation_compat"
  prohibitions:
    - statement: "No implicit output, XID/connector-ID equality, Xwayland, virtual output, pixel capture, modeset, or fallback is introduced."
      status: "resolved"
      verification: "CLI/process tests and live predicate require explicit scalar selection and query-only topology."
    - statement: "No reboot/remediation may occur before the archived original binary/evidence verifies."
      status: "resolved"
      verification: "The blocking checkpoint begins with verify-archive and reads archived bytes."
    - statement: "No successful HOST-02 run can clear capture, encoder, or overall G0 blockers."
      status: "resolved"
      verification: "Process/live readback asserts HOST-03/HOST-04 unproven and exit 2."
---

<objective>
Integrate explicit selected-output discovery/mapping into the doctor and prove
it on the corrected real Xorg host while retaining original V1 compatibility.

Purpose: Give the operator one durable selected physical output/GPU identity
before capture begins (D-01, D-02, D-03, D-06, D-09).

Output: `--output` CLI, live query/recheck, strict `selected-output.v1`,
process tests, archive compatibility, corrected-host readback, and docs.
</objective>

<execution_context>
@/home/finn/.codex/gsd-core/workflows/execute-plan.md
@/home/finn/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-host-readiness-gate/01-CONTEXT.md
@.planning/phases/01-host-readiness-gate/01-05-SUMMARY.md
@.planning/phases/01-host-readiness-gate/01-OUTPUT-GPU-MAPPING-SPIKE.md
@artifacts/validation/g0/pre-reboot/index.json
</context>

## Artifacts this phase produces

| Path | Contract |
|---|---|
| `src/cli.rs` | Optional single `--output <XRANDR_NAME>`; zero means discovery/FAIL, duplicate means usage error |
| `src/probe.rs` | Query-only bounded RandR/sysfs/NVML observation collector |
| `src/output_mapping.rs` | Pure predicate plus same-run topology recheck |
| `src/evidence.rs` | `selected-output.v1` strict known validator; generic unknown-extension behavior unchanged |
| `target/g0-post-reboot-output.json` | Fresh corrected-host HOST-01/HOST-02 readback, overall FAIL |

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Carry one selected output through the production command and V1 extension</name>
  <read_first>
    - `.planning/phases/01-host-readiness-gate/01-05-SUMMARY.md` — actual SelectedOutputV1/pure mapping exports
    - `.planning/phases/01-host-readiness-gate/01-OUTPUT-GPU-MAPPING-SPIKE.md` — authoritative live predicate
    - `.planning/phases/01-host-readiness-gate/01-04-SUMMARY.md` — archive and V1 compatibility commands
  </read_first>
  <files>src/cli.rs, src/lib.rs, src/probe.rs, src/evidence.rs, src/output_mapping.rs</files>
  <behavior>
    - Omitted output discovers exact candidates, writes current evidence, and exits 2; one exact name selects; repeated flags are usage error before probes.
    - A selected relation traverses bounded collection, pure mapping, topology recheck, evaluator, atomic V1 write, readback, and known-extension validation.
    - Any identity/topology difference removes selection.
  </behavior>
  <action>
Add the explicit CLI form and query-only RandR/sysfs/NVML collection. Persist
the full Plan 01-05 model in `selected-output.v1`, hash EDID in memory, and
requery output/provider/EDID/timing/DRM link/BDF/NVML UUID before admission.
Implement a strict known payload validator for this identifier while leaving
the V1 base and unknown-extension decoder untouched. Successful mapping clears
only HOST-02/provider correlation.
  </action>
  <verify><automated>bash -c 'cargo test --locked host02_cli_ &amp;&amp; cargo test --locked host02_live_collector_fixture_ &amp;&amp; cargo test --locked selected_output_extension_'</automated></verify>
  <acceptance_criteria>
    1. CLI grammar is deterministic and has no default selection.
    2. The spike predicate is implemented exactly.
    3. Topology recheck precedes extension PASS.
    4. Capture/NVENC/G0 remain failed.
  </acceptance_criteria>
  <done>One named output can traverse the permanent doctor/evidence path without schema mutation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Close mapping/process boundaries and extension compatibility</name>
  <read_first>
    - `src/cli.rs`, `src/probe.rs`, `src/output_mapping.rs`, `src/evidence.rs` — Task 1 integration
    - `tests/fixtures/host02-output-topologies.json` — Plan 01-05 matrix
    - `tests/fixtures/g0-envelope-v1-foundation.json` — permanent original V1 fixture
    - `artifacts/validation/g0/pre-reboot/index.json` — immutable original archive
  </read_first>
  <files>tests/host_doctor_cli.rs, README.md, .planning/phases/01-host-readiness-gate/01-VALIDATION.md</files>
  <action>
Run namespace-disjoint, identical-display, missing/ambiguous provider/
connector/BDF/NVML, invalid timing/name, topology race, timeout, stale/cache,
and secrecy cases through the production binary. Add the Plan 01-06 regression
command that decodes the foundation fixture and verifies the original archive
from its contained files. Document discovery, selection, fields, and remaining
G0 blockers.
  </action>
  <verify><automated>bash -c 'cargo test --locked --test host_doctor_cli host02_ &amp;&amp; cargo test --locked g0_v1_foundation_compat -- --exact &amp;&amp; cargo run --locked --bin replay-host-doctor -- verify-archive --index artifacts/validation/g0/pre-reboot/index.json'</automated></verify>
  <acceptance_criteria>
    1. Every mapping/race/cache/secrecy boundary has a production process test.
    2. A positive case uses unequal XRandR/DRM numeric IDs.
    3. Foundation fixture and original archive still verify.
    4. Docs make no capture/NVENC/G0 PASS claim.
  </acceptance_criteria>
  <done>HOST-02 integration is exhaustive and backward-compatible.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 3: Verify the archive, correct the host, and prove live HOST-01/HOST-02</name>
  <precondition>`artifacts/validation/g0/pre-reboot/index.json` exists and verify-archive succeeds against its contained binary/evidence/manifest.</precondition>
  <read_first>
    - `artifacts/validation/g0/pre-reboot/index.json` — immutable original archive
    - `.planning/phases/01-host-readiness-gate/01-VALIDATION.md` — exact corrected-host commands
    - `.planning/phases/01-host-readiness-gate/01-OUTPUT-GPU-MAPPING-SPIKE.md` — required relation
  </read_first>
  <files>None; operator remediation is outside the repository and doctor operations are read-only.</files>
  <action>
Run `verify-archive` first. The operator then reboots/selects the matching
installed kernel and logs into real local Xorg outside the workflow. Run
discovery, choose the exact `REPLAY_HOST_OUTPUT`, run the doctor, and verify
persisted evidence requires HOST-01 PASS, HOST-02 PASS, HOST-03 UNPROVEN,
HOST-04 UNPROVEN. Require a distinct corrected boot/session and overall exit 2.
  </action>
  <instructions>Verify immutable history first; perform the operator-only reboot/Xorg transition; then run discovery, exact selection, doctor, and persisted readback. Stop on any failed prerequisite.</instructions>
  <verify><automated>bash -c 'set -euo pipefail; cargo run --locked --bin replay-host-doctor -- verify-archive --index artifacts/validation/g0/pre-reboot/index.json; test -n "${REPLAY_HOST_OUTPUT:-}"; if cargo run --locked --bin replay-host-doctor -- run --output "$REPLAY_HOST_OUTPUT" --evidence target/g0-post-reboot-output.json; then code=0; else code=$?; fi; if [ "$code" -ne 2 ]; then exit 1; fi; cargo run --locked --bin replay-host-doctor -- verify-evidence --evidence target/g0-post-reboot-output.json --require-host01 pass --require-host02 pass --require-host03 unproven --require-host04 unproven --validate-extension selected-output.v1'</automated></verify>
  <verification>The archive verifier succeeds and current persisted evidence reports HOST-01/HOST-02 PASS, HOST-03/HOST-04 UNPROVEN, and overall G0 FAIL on a distinct corrected boot/session.</verification>
  <acceptance_criteria>
    1. Original archive verifies before remediation.
    2. Current readback—not environment strings—proves real Xorg, matching NVML, devices, and unique output mapping.
    3. Corrected identity differs from original boot/session.
    4. Overall G0 remains FAIL.
  </acceptance_criteria>
  <what-built>Explicit output selection, live mapping, durable extension evidence, and compatibility regressions.</what-built>
  <how-to-verify>Perform the operator-only reboot/Xorg transition, then run the exact automated command.</how-to-verify>
  <resume-signal>Type "ready" only after the archive and current HOST-01/HOST-02 readback pass.</resume-signal>
  <done>The corrected real host has one proven selected physical output and still-honest media blockers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| XRandR/sysfs/NVML → live mapper | Mutable separate namespaces enter selected-output evidence |
| Original archive → remediation | Historical failure must remain immutable |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-06-01 | Spoofing | Output/GPU | high | mitigate | Full unique relation, same-run topology recheck, ambiguity failure. |
| T-01-06-02 | Tampering | Extension evidence | high | mitigate | Strict known validator, payload digest, V1 compatibility regressions. |
| T-01-06-03 | Repudiation | Remediation order | high | mitigate | Verify immutable archived bytes before distinct corrected boot/session proof. |
| T-01-06-04 | Elevation of Privilege | Host correction | high | mitigate | Human-only reboot/login; doctor remains read/query-only. |
</threat_model>

<verification>
Run HOST-02 unit/process tests, foundation/archive compatibility, then the
archive-first corrected-host live command. Overall live exit must remain 2.
</verification>

<success_criteria>
- Exactly one physical output/GPU is proven on current real Xorg.
- Durable selected-output evidence is an extension, not a V1 base change.
- The original archive remains readable.
- G0 cannot pass before NvFBC and NVENC plans.
</success_criteria>

## Source coverage audit

| SOURCE | ID | Feature/requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Real selected-output prerequisite | 01-06 | COVERED | Corrected-host live proof and durable readback. |
| REQ | HOST-02 | Select/report one output | 01-06 | COVERED | CLI, live relation, exact fields. |
| RESEARCH | HostProbe | Query real current topology | 01-06 | COVERED | RandR/DRM/NVML current collection/recheck. |
| CONTEXT | D-01 | Fail closed | 01-06 | COVERED | Later blockers remain. |
| CONTEXT | D-02 | Exact runtime | 01-06 | COVERED | Live full relation required. |
| CONTEXT | D-03 | Exactly one output | 01-06 | COVERED | Explicit scalar CLI. |
| CONTEXT | D-06 | Pre/post remediation ordering | 01-06 | COVERED | Archive-first checkpoint. |
| CONTEXT | D-07 | No mutation by doctor | 01-06 | COVERED | Query-only; operator owns reboot/login. |
| CONTEXT | D-08 | Bounded | 01-06 | COVERED | Existing worker plus topology timeout tests. |
| CONTEXT | D-09 | Durable evidence | 01-06 | COVERED | selected-output.v1 and readback. |

No source-audit item is missing.

<output>
Create `.planning/phases/01-host-readiness-gate/01-06-SUMMARY.md` when done.
</output>
