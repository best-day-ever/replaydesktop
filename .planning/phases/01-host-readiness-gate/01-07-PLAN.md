---
phase: 01-host-readiness-gate
plan: 07
type: execute
wave: 7
depends_on: ["01-06"]
files_modified:
  - build.rs
  - native/nvfbc_abi_oracle.c
  - src/lib.rs
  - src/model.rs
  - src/probe.rs
  - src/evidence.rs
  - src/native_nvfbc.rs
  - tests/fixtures/host03-nvfbc-capture.json
  - tests/host_doctor_cli.rs
autonomous: true
requirements: [HOST-03]
must_haves:
  truths:
    - "A no-SDK build remains fixture-capable and exposes a live-unavailable provider; header/library/API presence never passes capture (D-02)."
    - "The conditional source gate and C oracle cover every NvFBC/CUDA declaration used before any future native call."
    - "CaptureFrameLeaseV1 and CopyLedgerV1 model one new selected-output shared-CUDA frame, ownership/copy/conversion edges, cursor semantics, post-processing, and cleanup without serializing pointers."
    - "Unknown, host-staged, disconnected, duplicate, cross-GPU-unproven, or cleanup-uncertain edges fail (D-05, D-08)."
    - "Diagnostic fixtures populate nvfbc-capture.v1 but can never produce live HOST-03 PASS."
  artifacts:
    - path: "src/native_nvfbc.rs"
      provides: "Source-gated provider seam and fixture/no-source implementations"
      exports: ["CaptureProvider", "FixtureCaptureProvider", "LiveUnavailableCaptureProvider", "capture_one_frame"]
    - path: "native/nvfbc_abi_oracle.c"
      provides: "Conditional official-header ABI comparisons"
    - path: "tests/fixtures/host03-nvfbc-capture.json"
      provides: "Source, state, frame, cursor, driver, ledger, timeout, and cleanup matrix"
  key_links:
    - from: "src/native_nvfbc.rs"
      to: "src/model.rs"
      via: "primitive observations become derived CaptureFrameLeaseV1/CopyLedgerV1"
      pattern: "CaptureFrameLeaseV1|CopyLedgerV1"
    - from: "src/evidence.rs"
      to: "src/model.rs"
      via: "strict nvfbc-capture.v1 validator is additive to the unchanged V1 base"
      pattern: "nvfbc-capture.v1"
  prohibitions:
    - statement: "No live reboot checkpoint, archive update, documentation claim, encoder allocation, alternate capture backend, or G0 PASS belongs to this plan."
      status: "resolved"
      verification: "Only conditional source/model/fixture files are changed and process tests require diagnostic provenance."
    - statement: "No native pointer, raw frame, proprietary source, operator path, or injected verdict/ledger is persisted."
      status: "resolved"
      verification: "Typed opaque IDs and sentinel/injection tests reject those fields."
    - statement: "No base V1 decoder or archive verifier change belongs to this isolated source/model/fixture plan."
      status: "resolved"
      verification: "Known-extension validation is additive, archive files are untouched, and compatibility is rerun at Plan 01-08 integration."
---

<objective>
Define and fixture-prove the NvFBC source/ABI boundary, one-frame lease,
copy-ledger, cursor/post-processing evidence, and cleanup matrix before any
operator/live capture step.

Purpose: Make HOST-03 behavior deterministic and safe while keeping source
absence and every unknown copy explicit (D-02, D-03, D-05, D-08, D-09).

Output: Conditional build/oracle, fixture/no-source providers, capture/ledger
models, strict extension validation, and process tests.
</objective>

<execution_context>
@/home/finn/.codex/gsd-core/workflows/execute-plan.md
@/home/finn/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/REQUIREMENTS.md
@.planning/research/STACK.md
@.planning/research/PITFALLS.md
@.planning/phases/01-host-readiness-gate/01-CONTEXT.md
@.planning/phases/01-host-readiness-gate/01-06-SUMMARY.md
</context>

## Artifacts this phase produces

| Path | Contract |
|---|---|
| `build.rs` | Conditional `REPLAY_NVFBC_SDK_ROOT`/`REPLAY_CUDA_SDK_ROOT` oracle build; no-source fixture build succeeds |
| `native/nvfbc_abi_oracle.c` | Includes official headers when supplied and exports project-owned comparisons only |
| `src/native_nvfbc.rs` | Provider seam and fixture/no-source state machines |
| `src/model.rs` | CapturePathEvidenceV1, CaptureFrameLeaseV1, CopyLedgerV1/EdgeV1, cleanup and cursor/post-processing facts |
| `src/evidence.rs` | Strict `nvfbc-capture.v1` payload validation without V1 base mutation |

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Carry one scripted shared-CUDA frame through production evidence</name>
  <read_first>
    - `.planning/phases/01-host-readiness-gate/01-06-SUMMARY.md` — actual selected-output/worker/evidence contracts
    - `.planning/phases/01-host-readiness-gate/01-CONTEXT.md` — D-02, D-03, D-05, D-08, D-09
    - `.planning/research/PITFALLS.md` — fallback/copy/session failure modes
  </read_first>
  <files>src/lib.rs, src/model.rs, src/probe.rs, src/evidence.rs, src/native_nvfbc.rs, tests/fixtures/host03-nvfbc-capture.json</files>
  <behavior>
    - A scripted primitive trace crosses selected-output identity, bounded Capture worker, derived lease/ledger, evaluator, V1 write/readback, and strict known-extension validation.
    - Fixture cannot supply run identity, admissibility, verdict, aggregate copy claim, or cleanup result.
    - NVENC boundary and overall G0 remain unproven.
  </behavior>
  <action>
Add `ProbeId::Capture` and bounded source/binding/CUDA/frame/surface/cursor/
cleanup/ledger types ending at `CaptureFrameLeaseV1`. Validate selected
topology/GPU identity, genuinely-new timestamp/status, dimensions, format,
pitch/planes, `bRequiredPostProcessing`, cursor inclusion/composition mode,
opaque surface identity, and reverse release ledger. Classify edges as
zero-copy, same-GPU device copy, peer copy, host-staged, conversion, or
unknown; derive totals in production and retain the NVENC boundary blocker.
  </action>
  <verify><automated>bash -c 'cargo test --locked host03_contract_ &amp;&amp; cargo test --locked host03_fixture_tracer_ &amp;&amp; cargo test --locked nvfbc_extension_'</automated></verify>
  <acceptance_criteria>
    1. Scripted observations traverse the permanent command/evidence path.
    2. Lease/ledger contains no address or raw frame.
    3. Unknown/host-staged/disconnected/identity-invalid edges fail.
    4. Diagnostic provenance retains HOST-03/G0 failure.
  </acceptance_criteria>
  <done>The architecture carries a derived capture lease/ledger without making a live claim.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the conditional source/ABI gate and cleanup state machine</name>
  <read_first>
    - `src/model.rs`, `src/native_nvfbc.rs`, `src/probe.rs` — Task 1 contracts
    - `.planning/research/STACK.md` — NvFBC shared-CUDA API 1.8 expectations
    - `src/digest.rs` — shared source digest contract
  </read_first>
  <files>build.rs, native/nvfbc_abi_oracle.c, src/native_nvfbc.rs, src/model.rs</files>
  <behavior>
    - Missing source builds the unavailable provider and performs no native call.
    - When official roots are supplied, the oracle compares every used layout/constant/version/status before provider enablement.
    - Failure after each modeled acquisition point releases initialized resources exactly once in reverse order.
  </behavior>
  <action>
Implement conditional source discovery only from explicit absolute roots. The
C oracle includes official NvFBC/CUDA headers and exports comparison values;
record source identity/digests without copying source. Model the exact
load/status/bind/create/grab/release/destroy/unload lifecycle and each inverse
cleanup edge. Add fixture cases for NvFBC ABI/driver-specific statuses,
capture denial/protected content/busy/no-frame, cursor modes, required
post-processing, stale frame, timeout, and partial initialization.
  </action>
  <verify><automated>bash -c 'cargo test --locked host03_no_source_ &amp;&amp; cargo test --locked host03_source_abi_fixture_ &amp;&amp; cargo test --locked host03_cleanup_'</automated></verify>
  <acceptance_criteria>
    1. No-source builds and tests pass without gated material.
    2. Source uncertainty blocks native provider enablement.
    3. Cursor and driver-specific behavior have explicit terminal evidence.
    4. Every partial acquisition has one reverse cleanup expectation.
  </acceptance_criteria>
  <done>The future live provider has a source-authenticated ABI and complete cleanup contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Exhaust fixture admission, copy, timeout, and secrecy boundaries</name>
  <read_first>
    - `tests/fixtures/host03-nvfbc-capture.json` — Tasks 1-2 matrix
    - `src/native_nvfbc.rs`, `src/evidence.rs`, `src/lib.rs` — production fixture path
  </read_first>
  <files>tests/fixtures/host03-nvfbc-capture.json, tests/host_doctor_cli.rs</files>
  <action>
Run source/runtime mismatch, alternate output/GPU/context, false alias,
peer-without-access, host-stage, unknown edge, malformed metadata, cursor/
post-processing, timeout, abnormal exit, every partial cleanup, cached PASS,
injected ledger/verdict, pointer/source/path/secret, XCB/PipeWire/software
fallback cases through the production binary. Require exit 2 and unchanged
original-envelope compatibility.
  </action>
  <verify><automated>bash -c 'cargo fmt --check &amp;&amp; cargo clippy --locked --all-targets -- -D warnings &amp;&amp; cargo test --locked host03_fixture_ &amp;&amp; cargo test --locked --test host_doctor_cli host03_diagnostic_'</automated></verify>
  <acceptance_criteria>
    1. All state/copy/cursor/driver/timeout/cleanup/admission boundaries are process tested.
    2. No fixture can produce live HOST-03 PASS.
    3. No archive/docs/live checkpoint is modified.
  </acceptance_criteria>
  <done>HOST-03 fixture behavior is complete and safely separated from live proof.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Optional SDK roots → build/oracle | External native declarations govern unsafe ABI |
| Fixture/native primitives → lease/ledger | Untrusted claims must be derived and bounded |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-07-01 | Tampering | NvFBC ABI | high | mitigate | Explicit roots, C oracle, source digest, unavailable provider on uncertainty. |
| T-01-07-02 | Tampering | Copy ledger | high | mitigate | Production-derived typed graph; reject unknown/host-stage/disconnected claims. |
| T-01-07-03 | Denial of Service | Capture lifecycle | high | mitigate | Modeled deadlines, bounded worker, reverse exactly-once cleanup matrix. |
| T-01-07-04 | Information Disclosure | Surface/source | high | mitigate | Opaque IDs/digests only; pointer/raw-frame/source/path sentinel tests. |
</threat_model>

<verification>
Run no-source, source-ABI fixture, contract, cleanup, process, fmt, and clippy
checks. Do not run live hardware or alter archives/docs.
</verification>

<success_criteria>
- Fixture and no-SDK paths are complete.
- Source/ABI, lease, copy, cursor, driver, and cleanup contracts are explicit.
- Live capture and G0 remain unavailable until Plan 01-08.
</success_criteria>

## Source coverage audit

| SOURCE | ID | Feature/requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Selected-output capture prerequisite | 01-07 | COVERED | Permanent model/provider path before live work. |
| REQ | HOST-03 | NvFBC path and copy reporting | 01-07 | COVERED | Fixture/ledger contract; live proof follows in 01-08. |
| RESEARCH | NvFBC flag | Surface ownership, cursor, driver-specific behavior | 01-07 | COVERED | Explicit models and fixtures. |
| CONTEXT | D-02 | Runtime over presence | 01-07 | COVERED | Presence/no-source cannot pass. |
| CONTEXT | D-03 | Selected output | 01-07 | COVERED | Lease binds selected topology/GPU. |
| CONTEXT | D-05 | Every copy known | 01-07 | COVERED | Typed graph and unknown blocker. |
| CONTEXT | D-08 | Timeouts/cleanup | 01-07 | COVERED | Full state/cleanup matrix. |
| CONTEXT | D-09 | Durable evidence | 01-07 | COVERED | Strict nvfbc-capture.v1 payload. |

No source-audit item is missing.

<output>
Create `.planning/phases/01-host-readiness-gate/01-07-SUMMARY.md` when done.
</output>
