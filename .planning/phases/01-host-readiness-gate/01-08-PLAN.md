---
phase: 01-host-readiness-gate
plan: 08
type: execute
wave: 8
depends_on: ["01-07"]
files_modified:
  - src/lib.rs
  - src/probe.rs
  - src/evidence.rs
  - src/native_nvfbc.rs
  - tests/host_doctor_cli.rs
  - README.md
  - .planning/phases/01-host-readiness-gate/01-VALIDATION.md
autonomous: false
requirements: [HOST-03]
user_setup:
  - service: nvidia-capture-and-cuda-sources
    why: "Live NvFBC/CUDA declarations and semantics require official operator-controlled sources and a separate lawful-source assertion."
    env_vars:
      - name: REPLAY_NVFBC_SDK_ROOT
        source: "Absolute readable official NVIDIA Capture SDK source root after applicable terms are accepted outside this workflow."
      - name: REPLAY_CUDA_SDK_ROOT
        source: "Absolute readable matching official CUDA header/documentation root."
      - name: REPLAY_HOST_OUTPUT
        source: "Exact output name already proven by the corrected HOST-02 run."
    dashboard_config:
      - task: "Confirm source identity and terms separately; the executor must not download, install, vendor, or accept gated material."
        location: "Operator-controlled NVIDIA source and physical Linux host"
must_haves:
  truths:
    - "The operator source/license assertion is separate from automated archive and current HOST-01/HOST-02 checks."
    - "The original archived binary/evidence and foundation V1 fixture verify before live capture."
    - "One current selected-output NvFBC shared-CUDA session obtains exactly one genuinely new frame, validates binding/cursor/post-processing/copy evidence, and cleans up (D-02, D-03, D-05, D-08)."
    - "nvfbc-capture.v1 passes only for live current provenance bound to the same output/GPU/topology."
    - "G0 remains FAIL solely with HOST-04/NVENC copy/tuple blockers."
  artifacts:
    - path: "src/native_nvfbc.rs"
      provides: "Official-source-backed live selected-output one-frame provider"
    - path: "target/g0-host03-live.json"
      provides: "Fresh current HOST-01/02/03 PASS, HOST-04 UNPROVEN readback"
    - path: ".planning/phases/01-host-readiness-gate/01-VALIDATION.md"
      provides: "Archive/source/current-host/live capture commands"
  key_links:
    - from: "src/probe.rs"
      to: "src/native_nvfbc.rs"
      via: "bounded Capture worker owns all source-verified NvFBC/CUDA calls"
      pattern: "ProbeId::Capture|NativeNvfbcCaptureProvider"
    - from: "src/native_nvfbc.rs"
      to: "src/output_mapping.rs"
      via: "pre/post capture checks require the same selected topology, PCI BDF, NVML UUID, and CUDA device"
      pattern: "SelectedOutputV1|topology"
  prohibitions:
    - statement: "No fixture, environment-only check, library/API presence, alternate output/GPU, XCB/PipeWire/software path, continuous capture, or encoder allocation can pass HOST-03."
      status: "resolved"
      verification: "Checkpoint/live/process tests require the full current one-frame path and retain HOST-04."
    - statement: "No pointer, raw frame, proprietary source/path, or opaque NVENC copy claim is persisted."
      status: "resolved"
      verification: "Evidence uses typed IDs/digests/ledger only and leaves NVENC boundary unproven."
---

<objective>
Cross the operator/source gate and prove one real selected-output NvFBC
shared-CUDA frame with complete application-visible copy and cleanup evidence.

Purpose: Close HOST-03 honestly while preserving the original failure and
keeping the still-unproven NVENC boundary visible (D-01, D-02, D-03, D-05,
D-06, D-07, D-08, D-09).

Output: Verified sources/preconditions, live provider, one-frame evidence,
strict extension PASS, regressions, docs, and overall G0 FAIL.
</objective>

<execution_context>
@/home/finn/.codex/gsd-core/workflows/execute-plan.md
@/home/finn/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/REQUIREMENTS.md
@.planning/research/STACK.md
@.planning/phases/01-host-readiness-gate/01-CONTEXT.md
@.planning/phases/01-host-readiness-gate/01-07-SUMMARY.md
@artifacts/validation/g0/pre-reboot/index.json
</context>

## Artifacts this phase produces

| Path | Contract |
|---|---|
| `src/native_nvfbc.rs` | Source/ABI/runtime verified selected-output session and one grab |
| `src/probe.rs` | Inner native deadlines below bounded Capture worker deadline |
| `src/evidence.rs` | Live/current strict `nvfbc-capture.v1` admission |
| `target/g0-host03-live.json` | Current readback with HOST-01..03 PASS, HOST-04 UNPROVEN, overall FAIL |

<tasks>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 1: Verify immutable history, sources, and current HOST-01/HOST-02</name>
  <precondition>The corrected host/output from Plan 01-06 remains current and the pre-reboot archive exists.</precondition>
  <read_first>
    - `artifacts/validation/g0/pre-reboot/index.json` — original archive
    - `.planning/phases/01-host-readiness-gate/01-07-SUMMARY.md` — fixture/source/ABI contracts
    - `.planning/phases/01-host-readiness-gate/01-VALIDATION.md` — exact commands
  </read_first>
  <files>None; this checkpoint changes no repository or host state.</files>
  <action>
First verify the original archive and foundation fixture. Separately obtain
the operator's assertion that both official source roots are authentic and
lawfully available; directory presence does not imply that assertion. Then run
the read-only doctor and persisted-evidence verifier requiring current
HOST-01/HOST-02 PASS on `REPLAY_HOST_OUTPUT`, with HOST-03/HOST-04 unproven.
Do not perform remediation, download, install, or license acceptance.
  </action>
  <instructions>Obtain the separate operator source assertion, verify original history, and run only the stated read-only current-host checks. Stop on any failed archive, source, HOST-01, or HOST-02 condition.</instructions>
  <verify><automated>bash -c 'set -euo pipefail; cargo test --locked g0_v1_foundation_compat -- --exact; cargo run --locked --bin replay-host-doctor -- verify-archive --index artifacts/validation/g0/pre-reboot/index.json; test -n "${REPLAY_NVFBC_SDK_ROOT:-}"; test -n "${REPLAY_CUDA_SDK_ROOT:-}"; test -n "${REPLAY_HOST_OUTPUT:-}"; if cargo run --locked --bin replay-host-doctor -- run --output "$REPLAY_HOST_OUTPUT" --evidence target/g0-host03-precondition.json; then code=0; else code=$?; fi; if [ "$code" -ne 2 ]; then exit 1; fi; cargo run --locked --bin replay-host-doctor -- verify-evidence --evidence target/g0-host03-precondition.json --require-host01 pass --require-host02 pass --require-host03 unproven --require-host04 unproven'</automated></verify>
  <verification>The foundation fixture and original archive verify, the separate source assertion is recorded, and current persisted evidence proves HOST-01/HOST-02 with later gates unproven.</verification>
  <acceptance_criteria>
    1. Original archived bytes and foundation fixture verify.
    2. Operator assertion is explicit and separate from path checks.
    3. Current readback proves HOST-01/HOST-02; no environment-only admission.
    4. Checkpoint performs no host mutation.
  </acceptance_criteria>
  <what-built>Fixture-complete NvFBC contracts and unavailable live provider.</what-built>
  <how-to-verify>Supply the separate source assertion and run the exact read-only command.</how-to-verify>
  <resume-signal>Type "ready" after archive, sources, and current HOST-01/HOST-02 all verify.</resume-signal>
  <done>Authenticated sources and current selected-output prerequisites are available without losing history.</done>
</task>

<task type="tracer" tdd="true">
  <name>Task 2: Acquire, validate, and clean up one real selected-output frame</name>
  <precondition>Task 1 passes and both official source roots remain readable.</precondition>
  <read_first>
    - `$REPLAY_NVFBC_SDK_ROOT` and `$REPLAY_CUDA_SDK_ROOT` — asserted official headers/docs
    - `src/native_nvfbc.rs`, `src/probe.rs`, `src/evidence.rs` — Plan 01-07 contracts
    - `native/nvfbc_abi_oracle.c`, `build.rs` — source/ABI gate
  </read_first>
  <files>src/lib.rs, src/probe.rs, src/evidence.rs, src/native_nvfbc.rs, tests/host_doctor_cli.rs</files>
  <behavior>
    - Pre-call source/oracle/runtime checks pass before session creation.
    - Exactly one grab returns a new selected-output frame before its inner deadline.
    - Failure after every acquisition point executes inverse cleanup once in reverse order.
    - Live extension PASS clears HOST-03 only.
  </behavior>
  <action>
Enable the native provider only after the conditional build oracle and runtime
symbols agree. Revalidate selected topology and NVML/CUDA GPU, create the
official shared-CUDA session bound to that output, issue exactly one grab,
validate timestamp/status/dimensions/format/pitch/planes/post-processing/cursor
facts, derive lease/copy/cleanup evidence, revalidate topology/GPU, and release
all resources once in reverse order. Use per-native-call deadlines shorter
than the outer worker deadline and do not retry/substitute a failed frame.
  </action>
  <verify>
    <automated>bash -c 'cargo test --locked host03_source_abi_ &amp;&amp; cargo test --locked host03_fixture_ &amp;&amp; cargo test --locked --test host_doctor_cli host03_process_'</automated>
    <live>timeout 180s cargo test --locked --test host_doctor_cli host03_live_one_frame_current_selected_output -- --ignored --exact --nocapture</live>
  </verify>
  <acceptance_criteria>
    1. Source/ABI/runtime identity is proven before native allocation.
    2. One genuinely new frame binds to the selected output/GPU.
    3. Cursor, post-processing, every application-visible edge, and cleanup are terminally evidenced.
    4. HOST-04 and overall G0 remain failed.
  </acceptance_criteria>
  <done>HOST-03 is backed by one real bounded selected-output NvFBC frame or remains explicitly blocked.</done>
</task>

<task type="auto">
  <name>Task 3: Publish live readback and rerun original compatibility</name>
  <read_first>
    - `src/native_nvfbc.rs`, `src/evidence.rs`, `tests/host_doctor_cli.rs` — Task 2 path
    - `artifacts/validation/g0/pre-reboot/index.json` — original archive
    - `tests/fixtures/g0-envelope-v1-foundation.json` — original fixture
  </read_first>
  <files>tests/host_doctor_cli.rs, README.md, .planning/phases/01-host-readiness-gate/01-VALIDATION.md</files>
  <action>
Run one fresh production doctor/readback on the selected output and require
HOST-01/02/03 PASS, HOST-04 UNPROVEN, exit 2. Rerun foundation-fixture and
original-archive compatibility with the extended binary. Document exact
source, static/fixture, live timeout, evidence fields, and remaining NVENC
boundary without a G0 PASS claim.
  </action>
  <verify><automated>bash -c 'set -euo pipefail; test -n "${REPLAY_HOST_OUTPUT:-}"; if cargo run --locked --bin replay-host-doctor -- run --output "$REPLAY_HOST_OUTPUT" --evidence target/g0-host03-live.json; then code=0; else code=$?; fi; if [ "$code" -ne 2 ]; then exit 1; fi; cargo run --locked --bin replay-host-doctor -- verify-evidence --evidence target/g0-host03-live.json --require-host01 pass --require-host02 pass --require-host03 pass --require-host04 unproven --validate-extension nvfbc-capture.v1; cargo test --locked g0_v1_foundation_compat -- --exact; cargo run --locked --bin replay-host-doctor -- verify-archive --index artifacts/validation/g0/pre-reboot/index.json'</automated></verify>
  <acceptance_criteria>
    1. Current live readback proves HOST-03 only through the known extension.
    2. Original fixture/archive remain readable.
    3. Docs separate static/fixture/live commands and retain HOST-04/G0 failure.
  </acceptance_criteria>
  <done>Live HOST-03 evidence is durable, backward-compatible, and honest about the remaining encoder boundary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Operator roots → native provider | External proprietary ABI/semantics enter unsafe code |
| Xorg/NvFBC/CUDA → worker | Mutable native resources and hangs enter evidence |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-08-01 | Tampering | Source/ABI | high | mitigate | Separate assertion, source digests/map, C oracle, runtime checks. |
| T-01-08-02 | Spoofing | Capture binding | high | mitigate | Current HOST-01/02 and pre/post output/GPU/context checks. |
| T-01-08-03 | Denial of Service | Native calls | high | mitigate | Per-call/worker deadlines, one grab, reverse cleanup matrix. |
| T-01-08-04 | Information Disclosure | Frame/source/pointer | high | mitigate | Typed metadata/digests/opaque IDs only, sentinel tests. |
</threat_model>

<verification>
Verify history/sources/current HOST-01/02, run static/fixture suites, then the
independent 180-second live one-frame proof and current readback.
</verification>

<success_criteria>
- One selected-output shared-CUDA frame is source/runtime proven and cleaned.
- Cursor/post-processing/copy facts are explicit.
- Original V1 history remains readable.
- G0 remains FAIL because HOST-04 is unproven.
</success_criteria>

## Source coverage audit

| SOURCE | ID | Feature/requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Real selected-output capture | 01-08 | COVERED | Source/live one-frame proof. |
| REQ | HOST-03 | NvFBC shared-CUDA and copy reporting | 01-08 | COVERED | Live lease/ledger/cleanup. |
| RESEARCH | NvFBC flag | Cursor, ownership, driver behavior | 01-08 | COVERED | Live evidence plus fixture matrix. |
| CONTEXT | D-01 | Fail closed | 01-08 | COVERED | HOST-04 remains. |
| CONTEXT | D-02 | Exact runtime | 01-08 | COVERED | Full source/session/frame sequence. |
| CONTEXT | D-03 | Selected output | 01-08 | COVERED | Same output/GPU required. |
| CONTEXT | D-05 | Every copy known | 01-08 | COVERED | Application-visible ledger; NVENC boundary stays open. |
| CONTEXT | D-06 | Preserve original | 01-08 | COVERED | Archive compatibility first. |
| CONTEXT | D-07 | No mutation | 01-08 | COVERED | Read/capture only. |
| CONTEXT | D-08 | Timeouts/cleanup | 01-08 | COVERED | Per-call/worker deadlines and reverse cleanup. |
| CONTEXT | D-09 | Durable evidence | 01-08 | COVERED | nvfbc-capture.v1 and readback. |

No source-audit item is missing.

<output>
Create `.planning/phases/01-host-readiness-gate/01-08-SUMMARY.md` when done.
</output>
