---
phase: 01-host-readiness-gate
plan: 10
type: execute
wave: 10
depends_on: ["01-09"]
files_modified:
  - build.rs
  - native/nvenc_abi_oracle.c
  - src/native_nvenc.rs
  - src/probe.rs
  - src/evidence.rs
  - tests/host_doctor_cli.rs
  - README.md
  - .planning/phases/01-host-readiness-gate/01-VALIDATION.md
  - artifacts/validation/g0/post-repair/
autonomous: false
requirements: [HOST-04]
user_setup:
  - service: nvidia-video-codec-sdk-13.1
    why: "The standalone native HOST-04 ABI must be derived from an authenticated official SDK 13.1 nvEncodeAPI.h and matching documentation, not stale 12.1 literals or runtime symbols."
    env_vars:
      - name: REPLAY_NVENC_SDK_ROOT
        source: "Absolute readable operator-controlled official Video Codec SDK 13.1 source/include root after applicable terms are accepted outside this workflow."
      - name: REPLAY_HOST_OUTPUT
        source: "Exact current selected output already proven through HOST-03."
    dashboard_config:
      - task: "Confirm source authenticity and terms separately; do not ask the executor to download, install, vendor, or accept gated material."
        location: "Operator-controlled NVIDIA source and corrected physical Linux host"
must_haves:
  truths:
    - "The final binary first verifies the foundation fixture and original pre-reboot archived binary/evidence through the compatible V1 decoder."
    - "Official SDK 13.1 header plus C oracle owns every native type/layout/GUID/enum/structure/API-version value; the runtime must satisfy documented 13.1 driver/CUDA requirements."
    - "Current HOST-01..03 are re-proven before any live encoder allocation."
    - "Every eligible policy tuple receives an exact 3840x2160@60 one-frame attempt using the live capture lease, real registered/mapped resource, real bitstream, copy predicate, and complete cleanup; every policy position is terminal (D-02, D-04, D-05)."
    - "Final G0 PASS is emitted only when all current HOST-01..04, source/currentness/copy/stream/cleanup/readback gates are complete; otherwise a durable FAIL/BLOCKED artifact is archived."
    - "A separate post-repair run directory contains the current exact binary/evidence/manifest and never overwrites the pre-reboot archive."
  artifacts:
    - path: "native/nvenc_abi_oracle.c"
      provides: "Authenticated 13.1-header ABI/version comparisons"
    - path: "src/native_nvenc.rs"
      provides: "Bounded native exact-tuple provider"
      exports: ["NativeNvencProbeProvider", "probe_nvenc_tuple_one_frame"]
    - path: "artifacts/validation/g0/post-repair/"
      provides: "Separate immutable final current binary/evidence/manifest/index"
  key_links:
    - from: "src/native_nvenc.rs"
      to: "src/native_nvfbc.rs"
      via: "the worker owns the actual CaptureFrameLeaseV1 and CUDA context in-process"
      pattern: "CaptureFrameLeaseV1|register"
    - from: "src/native_nvenc.rs"
      to: "src/nvenc_bitstream.rs"
      via: "advertisement follows actual bounded payload inspection"
      pattern: "inspect_nvenc_bitstream"
    - from: "src/evidence.rs"
      to: "artifacts/validation/g0/pre-reboot/index.json"
      via: "final compatibility verifies original archived bytes before validating current known extensions"
      pattern: "verify_archive|validate-extension"
  prohibitions:
    - statement: "No stale 12.1 constant/hash, 0xC1 literal, invented API encoding, enumeration-only advertisement, software encoder, alternate output/GPU/context/format, host staging, unknown copy, or silent substitution can pass."
      status: "resolved"
      verification: "Oracle, source/runtime, tuple/copy/bitstream/cleanup process tests and final live assertions reject each condition."
    - statement: "No final PASS may derive from fixture, cache, pre-repair evidence, partial tuple attempts, or a failed compatibility check."
      status: "resolved"
      verification: "Final evaluator requires current live provenance, all terminal records, original-history verification, and same-run readback."
---

<objective>
Authenticate SDK 13.1, run native exact one-frame NVENC probes against the live
selected-output capture lease, produce the sole final G0 decision, and archive
the current result separately.

Purpose: Complete HOST-04 and G0 without stale ABI assumptions, false tuple or
copy claims, lost historical compatibility, or combined verification
timeouts (D-01, D-02, D-04 through D-09).

Output: Header-derived ABI/provider, live exact tuple attempts, final strict
`nvenc-tuples.v1`, original compatibility proof, current G0 readback, and
separate immutable post-repair archive.
</objective>

<execution_context>
@/home/finn/.codex/gsd-core/workflows/execute-plan.md
@/home/finn/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/REQUIREMENTS.md
@.planning/research/STACK.md
@.planning/phases/01-host-readiness-gate/01-CONTEXT.md
@.planning/phases/01-host-readiness-gate/01-09-SUMMARY.md
@.planning/phases/01-host-readiness-gate/01-NVENC-COPY-BOUNDARY-SPIKE.md
@artifacts/validation/g0/pre-reboot/index.json
</context>

The standalone official SDK 13.1 proof is required by the approved phase
revision. Preserve the project-research pin `nv-codec-headers n12.1.14.0` for
later Kyber/Kymedia integration and record that compatibility as unclaimed
here; do not rewrite the pinned Kyber tree.

## Artifacts this phase produces

| Path | Contract |
|---|---|
| `build.rs` | Conditional authenticated 13.1 oracle compile in addition to the existing NvFBC gate |
| `native/nvenc_abi_oracle.c` | Header-derived types/layouts/GUIDs/enums/structure and API versions/major-minor helpers |
| `src/native_nvenc.rs` | Same-process capture-lease registration/mapping and exact one-frame attempts |
| `src/evidence.rs` | Strict live/current `nvenc-tuples.v1` and sole final G0 admission |
| `artifacts/validation/g0/post-repair/<run-id>-<evidence-digest>/replay-host-doctor` | Exact current `/proc/self/exe` archive copy |
| `artifacts/validation/g0/post-repair/<run-id>-<evidence-digest>/g0-evidence.json` | Exact current final live V1 evidence |
| `artifacts/validation/g0/post-repair/<run-id>-<evidence-digest>/manifest.json` | Current command/identities/source/binary/evidence/attempt/copy/cleanup digests |
| `artifacts/validation/g0/post-repair/index.json` | Separate create-once final manifest pointer/digest |

<tasks>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 1: Assert SDK authenticity and verify original/current prerequisites</name>
  <precondition>Plan 01-08 completed with current HOST-01/HOST-02/HOST-03 PASS and the immutable pre-reboot archive still exists.</precondition>
  <read_first>
    - `artifacts/validation/g0/pre-reboot/index.json` — original archive
    - `tests/fixtures/g0-envelope-v1-foundation.json` — original V1 fixture
    - `.planning/phases/01-host-readiness-gate/01-08-SUMMARY.md` — current live capture result
    - `.planning/phases/01-host-readiness-gate/01-VALIDATION.md` — exact commands
  </read_first>
  <files>None; this checkpoint performs read-only verification and a separate operator assertion.</files>
  <action>
Obtain the operator's separate assertion that `REPLAY_NVENC_SDK_ROOT` is an
authentic official SDK 13.1 root with applicable terms accepted outside this
workflow. Then verify the foundation fixture, original archive contained
binary/evidence/manifest, and one fresh current doctor readback requiring
HOST-01/02/03 PASS and HOST-04 UNPROVEN. Directory presence alone does not
prove source authenticity/license.
  </action>
  <instructions>Record the separate SDK assertion, verify the original fixture/archive, and run the fresh current-host readback. Stop before native NVENC work on any failure.</instructions>
  <verify><automated>bash -c 'set -euo pipefail; test -n "${REPLAY_NVENC_SDK_ROOT:-}"; test -r "$REPLAY_NVENC_SDK_ROOT/nvEncodeAPI.h"; test -n "${REPLAY_HOST_OUTPUT:-}"; cargo test --locked g0_v1_foundation_compat -- --exact; cargo run --locked --bin replay-host-doctor -- verify-archive --index artifacts/validation/g0/pre-reboot/index.json; if cargo run --locked --bin replay-host-doctor -- run --output "$REPLAY_HOST_OUTPUT" --evidence target/g0-host04-precondition.json; then code=0; else code=$?; fi; if [ "$code" -ne 2 ]; then exit 1; fi; cargo run --locked --bin replay-host-doctor -- verify-evidence --evidence target/g0-host04-precondition.json --require-host01 pass --require-host02 pass --require-host03 pass --require-host04 unproven'</automated></verify>
  <verification>The SDK assertion, original V1 history, and fresh current HOST-01/HOST-02/HOST-03 persisted proof all succeed while HOST-04 remains unproven.</verification>
  <acceptance_criteria>
    1. Source assertion is separate and explicit.
    2. Original fixture/archive verify with the final code line.
    3. Current live HOST-01..03 readback passes on the same selected output/GPU.
    4. No fixture/cache/environment-only prerequisite is accepted.
  </acceptance_criteria>
  <what-built>Copy predicate, exact policy/parser, and diagnostic HOST-04 path.</what-built>
  <how-to-verify>Supply the SDK assertion and run the exact compatibility/current-host command.</how-to-verify>
  <resume-signal>Type "ready" only after SDK, original history, and current HOST-01..03 verify.</resume-signal>
  <done>Authenticated SDK 13.1 and a current proven capture context are available.</done>
</task>

<task type="tracer" tdd="true">
  <name>Task 2: Derive the 13.1 ABI and implement native exact-tuple attempts</name>
  <precondition>Task 1 passes and `REPLAY_NVENC_SDK_ROOT/nvEncodeAPI.h` remains the asserted official 13.1 source.</precondition>
  <read_first>
    - `$REPLAY_NVENC_SDK_ROOT/nvEncodeAPI.h` and matching official 13.1 docs — sole native ABI/version authority
    - `.planning/phases/01-host-readiness-gate/01-NVENC-COPY-BOUNDARY-SPIKE.md` — mandatory copy predicate
    - `.planning/phases/01-host-readiness-gate/01-09-SUMMARY.md` — exact policy/parser/provider seam
    - `build.rs`, `native/nvfbc_abi_oracle.c` — existing conditional oracle pattern
  </read_first>
  <files>build.rs, native/nvenc_abi_oracle.c, src/native_nvenc.rs, src/probe.rs, src/evidence.rs, tests/host_doctor_cli.rs</files>
  <behavior>
    - Every native value and compatibility result agrees with official header/oracle.
    - Each hardware-eligible tuple opens exact 4K60 config, registers/maps the actual lease, submits one forced keyframe, parses matching bytes, and cleans up.
    - Normal unsupported/ineligible results are terminal and non-advertised; timeout/identity/copy/stream/cleanup failure blocks final G0.
  </behavior>
  <action>
Create the source map/oracle for every used type, size, alignment, offset,
GUID, enum, structure version, API version, and major/minor comparison. Remove
recalled/stale encodings. Verify SDK 13.1 Linux driver/CUDA requirements and
`libnvidia-encode.so.1` before session creation.

Inside bounded tuple workers that own the actual capture lease/context, use
caps only as preliminary observations. For every eligible policy position,
open exact 3840x2160 60/1 configuration with P2/ULL, synchronous Linux encode,
no B frames/lookahead/reorder, one-frame VBV; register/map the exact resource;
append spike-required copy evidence; submit one forced keyframe; inspect and
hash/clear actual bytes; and release all resources once in reverse order.
Persist all seven terminal attempts and derive advertisements only from exact
successes.
  </action>
  <verify>
    <automated>bash -c 'cargo test --locked host04_source_abi_ &amp;&amp; cargo test --locked host04_policy_ &amp;&amp; cargo test --locked host04_bitstream_ &amp;&amp; cargo test --locked --test host_doctor_cli host04_fixture_ -- --nocapture'</automated>
  </verify>
  <acceptance_criteria>
    1. Authenticated header/oracle owns every native/version value.
    2. 12.x/13.0/13.1/incompatible oracle-derived fixtures are deterministic.
    3. H.264 High and the closed HEVC/AV1 rules are exact.
    4. Advertisement requires config/resource/copy/real-stream/cleanup success.
    5. Static/fixture checks are separate from the live timeout.
  </acceptance_criteria>
  <done>The native provider is source-authenticated and ready for the final live run.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Run final live G0, verify compatibility, and archive current evidence</name>
  <precondition>Task 2 static/fixture gates pass and the corrected selected-output host remains unchanged.</precondition>
  <read_first>
    - `src/native_nvenc.rs`, `src/probe.rs`, `src/evidence.rs` — Task 2 native path
    - `.planning/phases/01-host-readiness-gate/01-NVENC-COPY-BOUNDARY-SPIKE.md` — live copy command/predicate
    - `artifacts/validation/g0/pre-reboot/index.json` — immutable original history
    - `src/archive.rs` — create-once contained archive contract
  </read_first>
  <files>tests/host_doctor_cli.rs, README.md, .planning/phases/01-host-readiness-gate/01-VALIDATION.md, artifacts/validation/g0/post-repair/</files>
  <behavior>
    - The independent live test produces seven terminal attempts, real matching bitstreams for every advertisement, closed copy proof, cleanup, and one current decision.
    - PASS requires all current HOST extensions and base/currentness checks; any unknown produces archived FAIL/BLOCKED.
    - Post-repair archive is separate and original archive remains unchanged/readable.
  </behavior>
  <action>
Run the spike's live copy command and the complete doctor under a 900-second
outer timeout while each native call retains a shorter deadline. Validate
known host-foundation, selected-output, nvfbc-capture, and nvenc-tuples
extensions after generic V1/base/raw-digest validation. Run final foundation
and original-archive regressions.

Use the existing self-contained archive writer to copy the current
`/proc/self/exe`, exact final evidence, and current manifest into the separate
post-repair create-once root. Archive PASS only when every gate is known and
true; otherwise archive the exact durable blocker. Document separate
static/fixture/live commands and the unclaimed Kyber 12.1-header integration
boundary.
  </action>
  <verify>
    <automated>bash -c 'cargo fmt --check &amp;&amp; cargo clippy --locked --all-targets -- -D warnings &amp;&amp; cargo test --locked g0_v1_foundation_compat -- --exact &amp;&amp; cargo run --locked --bin replay-host-doctor -- verify-archive --index artifacts/validation/g0/pre-reboot/index.json'</automated>
    <live>timeout 900s cargo test --locked --test host_doctor_cli host04_live_g0_current_output -- --ignored --exact --nocapture</live>
  </verify>
  <acceptance_criteria>
    1. Original fixture/archive verify with the final binary.
    2. Every advertised tuple has exact current config/copy/bitstream/cleanup proof.
    3. Final evidence validates all four known extensions and same-run base/currentness.
    4. Post-repair archived binary/evidence/manifest verify from contained bytes and do not overwrite pre-reboot history.
    5. G0 PASS exists only if complete; any unresolved fact is durably non-passing.
  </acceptance_criteria>
  <done>The host has one immutable exact current G0 result, PASS only when every source/runtime/copy/tuple/history gate is complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| SDK 13.1 → build/oracle | External declarations/license/version semantics enter unsafe code |
| Capture lease/CUDA → NVENC | Live ownership crosses register/map/encode boundaries |
| Driver/bitstream → worker/evidence | Native status and encoded bytes determine advertisement/G0 |
| Current result → post-repair archive | Final evidence must remain self-contained and distinct |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-10-01 | Tampering | SDK/API ABI | high | mitigate | Separate assertion, header digest/source map, C oracle, runtime requirement checks. |
| T-01-10-02 | Spoofing | NVENC input | high | mitigate | Actual in-process lease, same GPU/context, attributes, registered/mapped identity, copy predicate. |
| T-01-10-03 | Tampering | Tuple/stream | high | mitigate | Closed policy, exact config, real bounded stream parsing, derived advertisements. |
| T-01-10-04 | Denial of Service | Native/parser | high | mitigate | Per-call/worker deadlines, independent live timeout, bounded parser, cleanup matrix. |
| T-01-10-05 | Repudiation | Final G0 | high | mitigate | Original-history check plus separate contained current binary/evidence/manifest and digests. |
| T-01-10-06 | Information Disclosure | Pointers/bitstream/source/path | high | mitigate | Opaque IDs, hash/parse/clear bytes, allowlisted evidence, sentinels. |
</threat_model>

<verification>
Run source/ABI, policy, parser, fixture, fmt, and clippy commands separately.
Then run the independent 900-second live test, validate original history, and
verify the post-repair contained archive.
</verification>

<success_criteria>
- SDK 13.1 native ABI and exact live tuple attempts are authenticated.
- Copy/bitstream/cleanup evidence gates every advertisement.
- Final binary reads the original V1 fixture/archive.
- Separate current archive contains the exact current binary/evidence.
- G0 PASS exists only with complete current HOST-01..04 proof.
</success_criteria>

## Source coverage audit

| SOURCE | ID | Feature/requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Complete selected-output encode prerequisites and G0 | 01-10 | COVERED | Native exact probes and final archive. |
| REQ | HOST-04 | Probe each advertised atomic tuple on actual GPU/driver | 01-10 | COVERED | Exact current attempts/bytes/copy/cleanup. |
| RESEARCH | NVENC/G0 | Generation matrix, one-frame proof, no hidden copy | 01-10 | COVERED | Authenticated 13.1 standalone probe; pinned Kyber integration remains separate. |
| CONTEXT | D-01 | Fail-closed G0 | 01-10 | COVERED | Sole complete current evaluator. |
| CONTEXT | D-02 | Runtime over presence | 01-10 | COVERED | Exact session/resource/stream required. |
| CONTEXT | D-04 | Atomic tuples | 01-10 | COVERED | Closed seven-position policy. |
| CONTEXT | D-05 | Every copy known | 01-10 | COVERED | Live spike predicate gates. |
| CONTEXT | D-06 | Preserve original/current distinction | 01-10 | COVERED | Pre/post archives separate. |
| CONTEXT | D-07 | No system mutation | 01-10 | COVERED | Operator-only source/host setup; probe is read/capture/encode. |
| CONTEXT | D-08 | Timeouts/cleanup | 01-10 | COVERED | Per-call, worker, outer timeout and reverse cleanup. |
| CONTEXT | D-09 | Durable exact evidence | 01-10 | COVERED | Known extensions plus contained current archive. |

No source-audit item is missing.

<output>
Create `.planning/phases/01-host-readiness-gate/01-10-SUMMARY.md` when done;
report BLOCKED if any source, history, host, copy, tuple, stream, cleanup, or
currentness gate remains unresolved.
</output>
