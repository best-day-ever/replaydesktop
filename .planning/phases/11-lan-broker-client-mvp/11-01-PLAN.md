---
phase: 11
plan: 01
type: tracer
wave: 1
depends_on: []
files_modified:
  - server/migrations/0002_host_presence.sql
  - server/src/api.rs
  - server/src/config.rs
  - server/src/domain.rs
  - server/src/main.rs
  - server/src/password.rs
  - server/src/store.rs
  - server/src/kyber_jwt.rs
  - server/tests/lan_broker.rs
  - server/Dockerfile
  - server/docker/entrypoint.sh
  - compose.lan.yaml
  - scripts/run-lan-broker.sh
  - scripts/verify-lan-broker.sh
  - prototype/macos/ReplayDesktopLauncher.swift
  - server/README.md
autonomous: true
requirements:
  - CTRL-01
  - CTRL-02
  - CTRL-03
  - CTRL-04
  - CTRL-05
  - AUTH-01
  - AUTH-02
must_haves:
  truths:
    - D-01 The broker authorizes and discovers; the selected LAN endpoint remains the Kyber data path.
    - D-03 LAN mode performs no UniFi or firewall mutation and production defaults remain fail-closed.
    - D-04 The weak test password exists only behind explicit insecure development bootstrap.
    - D-05 Stale hosts are offline and cannot start new sessions.
    - D-06 A broker login yields a Kyber-compatible JWT path with no second host login.
    - D-07 The broker URL is remembered and changeable before or after login.
    - D-09 Per-host settings and current statistics remain reachable while connected.
    - D-10 Direct Kyber launch uses the enrolled certificate fingerprint.
  prohibitions:
    - statement: Do not proxy media, input, audio, or clipboard through the broker.
      status: resolved
      verification: automated API and source assertions
    - statement: Do not enable UniFi or expose a committed production secret in the LAN compose profile.
      status: resolved
      verification: compose/source assertions
---

<objective>
Deliver the complete laptop-to-Mac LAN broker tracer: persistent broker login,
host heartbeat/discovery, broker-signed direct Kyber authorization, local Docker
runtime, and a broker-first Parsec-style AppKit client with remembered per-host
settings and accessible live statistics.
</objective>

<tasks>

<task type="auto">
  <name>Implement host presence and direct LAN session authorization</name>
  <read_first>server/src/api.rs, server/src/store.rs, server/src/domain.rs, server/src/config.rs, server/src/main.rs, server/src/password.rs, server/src/ticket.rs, server/migrations/0001_init.sql, upstream/kyber-desktop/kysdk/kyctl/kycontroller/src/auth/jwt.rs</read_first>
  <action>Add migration-backed host registration fields and grant registered hosts to the one organization; add authenticated heartbeat registration, online/offline workstation summaries, an explicit LAN connect endpoint, RS256 Kyber JWT issuance with aud=kyber/sub/iat/exp, host-token verification, and explicit allow-insecure-http/allow-insecure-dev-bootstrap gates. Keep UniFi as the default admission mode and keep normal password creation at the existing 14-byte minimum.</action>
  <acceptance_criteria>`cargo test --locked --manifest-path server/Cargo.toml` exits 0; login failure remains non-enumerating; stale host connect returns conflict; LAN connect returns the registered IP/port, fingerprint, and an RS256 token; normal `hash_password("1337")` still fails.</acceptance_criteria>
  <verify>cargo fmt --manifest-path server/Cargo.toml --check && cargo clippy --locked --manifest-path server/Cargo.toml --all-targets -- -D warnings && cargo test --locked --manifest-path server/Cargo.toml</verify>
</task>

<task type="auto">
  <name>Package and run the broker plus laptop registrar in Docker</name>
  <read_first>server/Cargo.toml, server/src/main.rs, server/README.md, /home/finn/.local/share/replaydesktop/kyber-spike.toml</read_first>
  <action>Create a multi-stage Dockerfile, generated-secret entrypoint, compose.lan.yaml, and run/verification scripts. Publish broker host port 8090 only; persist `/var/lib/replay-control`; generate rather than commit host-registration and RSA private keys; run a registrar heartbeat advertising 192.168.33.42:8080 and the current host certificate; explicitly select LAN/memory admission and insecure HTTP only in this profile. Enable the active host's RS256 Kyber backend with the generated public key and leave basic auth available as a recovery path.</action>
  <acceptance_criteria>`docker compose -f compose.lan.yaml ps` reports broker and registrar healthy/running; `/healthz` returns 204; `finn / 1337` logs in; `/v1/workstations` returns this laptop online; the signed connect response targets `192.168.33.42:8080`; no UniFi environment value is required.</acceptance_criteria>
  <verify>scripts/verify-lan-broker.sh</verify>
</task>

<task type="auto">
  <name>Implement the broker-first macOS workstation experience</name>
  <read_first>prototype/macos/ReplayDesktopLauncher.swift, scripts/package-macos-gui.sh, server/src/domain.rs, server/src/api.rs</read_first>
  <action>Add Codable broker DTOs and a URLSession client; add remembered broker URL, username/password login, refresh/logout, authorized machine list with online/offline state, selection/connect/settings controls, per-workstation UserDefaults namespaces, broker LAN-connect call, `--auth-token` and `--tls-fingerprint` child arguments, and a Session menu that opens settings/statistics and disconnects. Keep existing telemetry reducers and clearly state that changed transport settings apply to the next connection.</action>
  <acceptance_criteria>The source contains broker login/list/connect endpoints and no stored password key; selected host ID namespaces settings; offline machines cannot connect; KyclientArguments adds `--auth-token` and `--tls-fingerprint` before `--`; the Session menu exposes Settings & Statistics and Disconnect.</acceptance_criteria>
  <verify>swift source assertions pass in scripts/verify-lan-broker.sh; on macOS, scripts/package-macos-gui.sh builds the arm64 app and its existing self-test passes.</verify>
</task>

</tasks>

<verification>
- Run Rust format, clippy, and all server tests.
- Build and start the LAN Docker profile, then run the login-to-direct-session tracer.
- Decode the returned JWT header/claims and verify its signature through the broker test suite.
- Confirm the active Kyber listener remains 192.168.33.42:8080 and broker remains 192.168.33.42:8090.
- Run deterministic Swift source assertions; record that AppKit compilation still requires the target Mac/Xcode lane.
</verification>

<success_criteria>
- The requested `finn / 1337` login discovers this laptop and creates a one-login direct Kyber launch contract.
- Firewall admission is disabled by explicit LAN configuration, with no regression to the future UniFi path.
- The Mac remembers the broker and each machine's settings and can reach settings/stats/disconnect during a session.
- The broker never receives or proxies Kyber media/input/clipboard traffic.
</success_criteria>

## Artifacts this phase produces

- `HostRegistrationRequest`, `WorkstationSummary` presence fields, and `LanConnectionResponse` API DTOs.
- `Store::register_host`, `Store::list_workstations_with_presence`, and `Store::create_lan_connection`.
- `KyberJwtIssuer` and RS256 `KyberClaims`.
- `Command::RegisterHost`, LAN-mode and insecure-development CLI flags.
- `server/migrations/0002_host_presence.sql`, `server/Dockerfile`, `server/docker/entrypoint.sh`, `compose.lan.yaml`.
- `scripts/run-lan-broker.sh` and `scripts/verify-lan-broker.sh`.
- Broker DTO/client/session state and per-workstation preference keys in `ReplayDesktopLauncher.swift`.
